query Ability($language: Language) {
  constants {
    abilities(language: $language) {
      id
      name
      language {
        description
        attributes
        lore
        aghanimDescription
        shardDescription
        notes
      }
    }
  }
}
